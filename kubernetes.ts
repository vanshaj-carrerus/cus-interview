import { TopicData } from "@/app/practice/data/types";

export const jdbcMasteryData: TopicData = {
  slug: "jdbc-mastery",
  title: "JDBC Mastery",
  intro: "From establishing database connections to advanced batch processing, transactions, and connection pooling.",
  levels: [
    {
      level: 1,
      title: "Level 1: JDBC Basics & Architecture",
      description: "Introduction to Java Database Connectivity, drivers, and core interfaces.",
      passScore: 16,
      questions: [
        { id: "jdbc-1-1", question: "What does JDBC stand for?", options: ["Java Data Base Control", "Java Database Connectivity", "Java Direct Backend Connection", "Java Distributed Base Communication"], answerIndex: 1, explanation: "JDBC is a Java API that manages connecting to a database, issuing queries and commands, and handling result sets." },
        { id: "jdbc-1-2", question: "Which package contains the core JDBC classes and interfaces?", options: ["java.database", "javax.sql", "java.sql", "java.db"], answerIndex: 2, explanation: "The java.sql package contains the core API (like Connection, Statement, ResultSet), while javax.sql contains advanced features (like DataSource)." },
        { id: "jdbc-1-3", question: "How many types of JDBC drivers are there?", options: ["2", "3", "4", "5"], answerIndex: 2, explanation: "There are 4 types: JDBC-ODBC Bridge (Type 1), Native-API (Type 2), Network Protocol (Type 3), and Thin/Native Protocol (Type 4)." },
        { id: "jdbc-1-4", question: "Which JDBC driver type is purely written in Java and communicates directly with the database's proprietary protocol?", options: ["Type 1", "Type 2", "Type 3", "Type 4"], answerIndex: 3, explanation: "Type 4 (Thin Driver) converts JDBC calls directly into the vendor-specific database protocol." },
        { id: "jdbc-1-5", question: "Which of the following is NOT a core interface in the JDBC API?", options: ["Connection", "Statement", "Session", "ResultSet"], answerIndex: 2, explanation: "Session is not a core JDBC interface (it is used in frameworks like Hibernate). Connection, Statement, and ResultSet are core." },
        { id: "jdbc-1-6", question: "What is the primary role of the DriverManager class?", options: ["To execute SQL queries", "To manage a list of database drivers and establish connections", "To store the database credentials", "To parse ResultSets"], answerIndex: 1, explanation: "DriverManager attempts to select an appropriate driver from the set of registered JDBC drivers when a connection is requested." },
        { id: "jdbc-1-7", question: "What is a JDBC URL?", options: ["A website linking to JDBC documentation", "A string that the DriverManager uses to identify the database to connect to", "A driver file path", "An SQL command"], answerIndex: 1, explanation: "It typically follows the format: jdbc:subprotocol:subname." },
        { id: "jdbc-1-8", question: "In the URL 'jdbc:mysql://localhost:3306/mydb', what is 'mysql'?", options: ["The protocol", "The subprotocol", "The subname", "The driver class"], answerIndex: 1, explanation: "In 'jdbc:subprotocol:subname', 'mysql' is the subprotocol." },
        { id: "jdbc-1-9", question: "Prior to JDBC 4.0, which method was explicitly used to register a JDBC driver class?", options: ["DriverManager.register()", "Class.forName()", "System.loadDriver()", "Connection.init()"], answerIndex: 1, explanation: "Class.forName(\"com.mysql.cj.jdbc.Driver\") was used to dynamically load the driver class into memory." },
        { id: "jdbc-1-10", question: "What does the Connection interface represent?", options: ["An SQL statement", "A session with a specific database", "A row of data", "An error state"], answerIndex: 1, explanation: "A Connection object represents a physical connection to the database. You execute SQL statements within the context of a Connection." },
        { id: "jdbc-1-11", question: "Which interface provides methods for executing SQL statements and returning results?", options: ["Query", "ResultSet", "Statement", "Executor"], answerIndex: 2, explanation: "The Statement interface is used to send basic SQL statements to the database." },
        { id: "jdbc-1-12", question: "What is a ResultSet?", options: ["An array of SQL queries", "An object that maintains a cursor pointing to its current row of data produced by a query", "A database table", "An exception handler"], answerIndex: 1, explanation: "ResultSet represents the dataset generated by executing a SELECT query." },
        { id: "jdbc-1-13", question: "Why was the Type 1 (JDBC-ODBC Bridge) driver removed in Java 8?", options: ["It was too fast", "It required platform-specific native binaries (ODBC) to be installed on every client machine", "It only supported Oracle", "It used too much RAM"], answerIndex: 1, explanation: "Type 1 relies on ODBC, making it platform-dependent and generally slower due to multiple translation layers." },
        { id: "jdbc-1-14", question: "Which exception is thrown when a database access error occurs?", options: ["DatabaseException", "SQLException", "JDBCException", "DataAccessException"], answerIndex: 1, explanation: "SQLException provides information on a database access error or other errors." },
        { id: "jdbc-1-15", question: "What is the recommended standard way to close JDBC resources (Connection, Statement, ResultSet) to prevent memory leaks?", options: ["Garbage collection will handle it automatically", "Call System.exit()", "Using a finally block to explicitly close them, or using try-with-resources", "Closing the ResultSet closes everything"], answerIndex: 2, explanation: "Resources must be explicitly closed to free up database cursors and network connections." },
        { id: "jdbc-1-16", question: "What happens if you close a Connection object?", options: ["Only the connection closes", "Its active Statement and ResultSet objects are also automatically closed", "The database shuts down", "An exception is thrown if statements are still open"], answerIndex: 1, explanation: "However, it is a best practice to explicitly close ResultSets and Statements before the Connection." },
        { id: "jdbc-1-17", question: "What is the typical sequence of steps to query a database in JDBC?", options: ["Get ResultSet -> Get Connection -> Create Statement", "Create Statement -> Get Connection -> Execute -> Get ResultSet", "Get Connection -> Create Statement -> Execute Query -> Process ResultSet", "Register Driver -> Execute Query -> Get Connection"], answerIndex: 2, explanation: "You must connect, prepare the command, run it, and then read the results." },
        { id: "jdbc-1-18", question: "Which JDBC driver type is known as a 'Network Protocol' driver?", options: ["Type 1", "Type 2", "Type 3", "Type 4"], answerIndex: 2, explanation: "Type 3 drivers use a middle-tier server that translates JDBC calls into a database-specific protocol." },
        { id: "jdbc-1-19", question: "In modern Java (JDBC 4.0+), why is `Class.forName()` usually no longer required?", options: ["Drivers register themselves automatically via the Service Provider Mechanism (SPI)", "Java no longer requires drivers", "The database automatically connects to Java", "Compilers link drivers statically"], answerIndex: 0, explanation: "If the driver JAR is in the classpath, DriverManager finds it via `META-INF/services/java.sql.Driver`." },
        { id: "jdbc-1-20", question: "What is the default auto-commit behavior for a new JDBC Connection?", options: ["false", "true", "Depends on the database", "Depends on the driver"], answerIndex: 1, explanation: "By default, every single SQL statement is treated as a transaction and is automatically committed right after it is executed." }
      ]
    },
    {
      level: 2,
      title: "Level 2: Establishing Connections & Driver Management",
      description: "Deep dive into JDBC URLs, DriverManager, Properties, and connecting securely.",
      passScore: 16,
      questions: [
        { id: "jdbc-2-1", question: "Which method in DriverManager is used to establish a connection?", options: ["createConnection()", "connect()", "getConnection()", "open()"], answerIndex: 2, explanation: "DriverManager.getConnection(url, username, password) returns a Connection object." },
        { id: "jdbc-2-2", question: "Which signature of getConnection() allows passing connection arguments (like SSL settings) easily?", options: ["getConnection(String url)", "getConnection(String url, String user, String pass)", "getConnection(String url, Properties info)", "All of the above"], answerIndex: 3, explanation: "The Properties object allows you to pass many arbitrary key-value pairs to the driver." },
        { id: "jdbc-2-3", question: "If you have multiple JDBC drivers in your classpath that can accept the same URL, which one does DriverManager choose?", options: ["The one loaded first", "It throws an ambiguity exception", "The newest driver", "It prompts the user"], answerIndex: 0, explanation: "DriverManager asks each loaded driver if it understands the URL, and uses the first one that says yes." },
        { id: "jdbc-2-4", question: "What does the `setLoginTimeout(int seconds)` method in DriverManager do?", options: ["Sets the maximum time a query can run", "Sets the maximum time to wait while attempting to connect to a database", "Sets how long a user session lasts", "Sets the delay before the driver is unloaded"], answerIndex: 1, explanation: "If the database is unreachable, the driver will stop trying after this timeout." },
        { id: "jdbc-2-5", question: "Why is placing database credentials directly in the Java source code considered a bad practice?", options: ["It causes compile errors", "It violates security best practices (hardcoding secrets)", "It slows down connection time", "The DriverManager ignores them"], answerIndex: 1, explanation: "Credentials should be externalized into environment variables, configuration files, or a secret manager." },
        { id: "jdbc-2-6", question: "How does the Service Provider Interface (SPI) help JDBC 4.0?", options: ["It encrypts the connection", "It automatically discovers and loads Driver implementations found in the classpath", "It writes the SQL queries", "It manages connection pooling"], answerIndex: 1, explanation: "This removes the need for `Class.forName()`." },
        { id: "jdbc-2-7", question: "If you get a `java.sql.SQLRecoverableException`, what does it imply?", options: ["The query was malformed", "The connection failed but might succeed if the application retries the operation", "The driver is missing", "The database is deleted"], answerIndex: 1, explanation: "A recoverable exception means the application can try again (e.g., a temporary network blip)." },
        { id: "jdbc-2-8", question: "Which interface provides a factory for connections to the physical data source, serving as an alternative to DriverManager?", options: ["ConnectionFactory", "DriverFactory", "DataSource", "PoolManager"], answerIndex: 2, explanation: "DataSource is part of javax.sql and is the preferred, modern way to obtain connections." },
        { id: "jdbc-2-9", question: "What is a major advantage of using DataSource over DriverManager?", options: ["It writes SQL for you", "It natively supports Connection Pooling and Distributed Transactions", "It requires no URL", "It is part of Java SE"], answerIndex: 1, explanation: "DataSource is typically configured in an application server and abstracts the connection details." },
        { id: "jdbc-2-10", question: "In a JDBC URL like `jdbc:postgresql://dbserver:5432/testdb`, what is `testdb`?", options: ["The username", "The driver name", "The specific database/schema name to connect to", "The protocol"], answerIndex: 2, explanation: "It identifies the specific database instance on the server." },
        { id: "jdbc-2-11", question: "What happens if `DriverManager.getConnection()` fails?", options: ["It returns null", "It throws a SQLException", "It retries infinitely", "It exits the JVM"], answerIndex: 1, explanation: "You must catch or declare this checked exception." },
        { id: "jdbc-2-12", question: "What does `Connection.isValid(int timeout)` do?", options: ["Checks if the URL is formatted correctly", "Pings the database to verify if the connection is still alive and working", "Validates a SQL query", "Checks if the driver is loaded"], answerIndex: 1, explanation: "It returns true if the connection has not been closed and is still valid." },
        { id: "jdbc-2-13", question: "How do you specify the character encoding (e.g., UTF-8) when establishing a MySQL JDBC connection?", options: ["Append it to the URL (e.g., ?characterEncoding=UTF-8)", "Pass it as the username", "Call Connection.setEncoding()", "Use the SET_CHARSET SQL command"], answerIndex: 0, explanation: "Parameters like useSSL=false or characterEncoding=UTF-8 are passed via the query string of the JDBC URL or via a Properties object." },
        { id: "jdbc-2-14", question: "Can a single Java application connect to multiple different databases (e.g., MySQL and Oracle) simultaneously?", options: ["No, JDBC only supports one driver at a time", "Yes, by loading both drivers and using different JDBC URLs", "Only if they share the same schema", "Only using Type 1 drivers"], answerIndex: 1, explanation: "DriverManager can maintain connections to diverse databases using their respective URLs." },
        { id: "jdbc-2-15", question: "What is a `java.sql.SQLFeatureNotSupportedException`?", options: ["The SQL syntax is wrong", "The JDBC driver does not support an optional JDBC feature that was called", "The Java version is too old", "The database is turned off"], answerIndex: 1, explanation: "Not all drivers implement every method in the JDBC specification." },
        { id: "jdbc-2-16", question: "If you call `DriverManager.getDrivers()`, what does it return?", options: ["An array of strings", "An Enumeration of all currently loaded JDBC drivers", "A single Driver object", "An integer count"], answerIndex: 1, explanation: "It allows you to inspect which drivers the JVM has successfully registered." },
        { id: "jdbc-2-17", question: "What is the purpose of the Driver.connect() method?", options: ["It is called by the developer directly", "It is called by DriverManager to attempt to make a database connection to the given URL", "It closes the connection", "It executes a query"], answerIndex: 1, explanation: "Developers rarely call Driver.connect() directly; DriverManager does this behind the scenes." },
        { id: "jdbc-2-18", question: "What does `Connection.isClosed()` check?", options: ["If the database server is down", "If the Connection object has had its close() method called", "If the query finished", "If the firewall blocked the connection"], answerIndex: 1, explanation: "It does not necessarily ping the server; it just checks the local state of the object." },
        { id: "jdbc-2-19", question: "What happens if you omit the port number in a JDBC URL?", options: ["It throws an error", "The driver connects using the database vendor's default port (e.g., 3306 for MySQL, 5432 for PostgreSQL)", "It uses port 80", "It uses port 0"], answerIndex: 1, explanation: "Drivers are hardcoded with their respective database's standard default ports." },
        { id: "jdbc-2-20", question: "Why is an unclosed JDBC connection dangerous?", options: ["It corrupts the Java code", "It can exhaust the database server's connection limit, preventing new connections (Connection Leak)", "It deletes tables", "It causes compilation errors"], answerIndex: 1, explanation: "Databases have a finite number of connection slots. Leaking them eventually crashes the application." }
      ]
    },
    {
      level: 3,
      title: "Level 3: Statements & PreparedStatements",
      description: "Executing SQL, preventing SQL Injection, and binding parameters.",
      passScore: 16,
      questions: [
        { id: "jdbc-3-1", question: "Which method of the Connection interface creates a standard Statement object?", options: ["prepareStatement()", "createStatement()", "executeStatement()", "newStatement()"], answerIndex: 1, explanation: "Connection.createStatement() returns a basic Statement object." },
        { id: "jdbc-3-2", question: "What is the primary vulnerability of using regular Statement objects with concatenated user input?", options: ["Cross-Site Scripting (XSS)", "SQL Injection", "Buffer Overflow", "Null Pointer Exception"], answerIndex: 1, explanation: "Concatenating user input directly into a SQL string allows attackers to manipulate the query logic." },
        { id: "jdbc-3-3", question: "Which interface extends Statement to execute precompiled SQL statements with parameters?", options: ["PreparedStatement", "CallableStatement", "ParameterizedStatement", "QueryStatement"], answerIndex: 0, explanation: "PreparedStatement represents a precompiled SQL statement that can efficiently execute multiple times with different parameters." },
        { id: "jdbc-3-4", question: "How are parameters denoted in a PreparedStatement SQL string?", options: ["Using $1, $2", "Using @param", "Using ? (question marks)", "Using {0}, {1}"], answerIndex: 2, explanation: "Question marks (?) act as placeholders for values that will be bound later." },
        { id: "jdbc-3-5", question: "In PreparedStatement parameter binding, what is the index of the FIRST parameter?", options: ["0", "1", "-1", "Depends on the driver"], answerIndex: 1, explanation: "Unlike Java arrays which are 0-indexed, JDBC parameter indexes are 1-based." },
        { id: "jdbc-3-6", question: "Which method binds a string value to the first parameter of a PreparedStatement?", options: ["pstmt.set(1, \"John\");", "pstmt.setString(\"John\");", "pstmt.setString(1, \"John\");", "pstmt.setParameter(0, \"John\");"], answerIndex: 2, explanation: "You must specify the index (1) and the value (\"John\")." },
        { id: "jdbc-3-7", question: "Why is PreparedStatement faster than Statement when executing the same query multiple times?", options: ["It uses a faster network protocol", "The database compiles the SQL syntax and execution plan once, and reuses it for subsequent executions", "It runs in parallel", "It bypasses the database engine"], answerIndex: 1, explanation: "Pre-compilation drastically reduces the overhead of query parsing and planning." },
        { id: "jdbc-3-8", question: "What does Statement.executeQuery(String sql) return?", options: ["int", "boolean", "ResultSet", "void"], answerIndex: 2, explanation: "executeQuery is used for SELECT statements and returns a ResultSet." },
        { id: "jdbc-3-9", question: "What does Statement.executeUpdate(String sql) return?", options: ["A ResultSet", "A boolean", "An integer representing the number of rows affected (inserted/updated/deleted)", "The ID of the inserted row"], answerIndex: 2, explanation: "It is used for DML operations (INSERT, UPDATE, DELETE) and DDL operations (which return 0)." },
        { id: "jdbc-3-10", question: "When would you use the generic Statement.execute(String sql) method?", options: ["When you only want to update data", "When executing a query whose result type (ResultSet or update count) is unknown until runtime", "When you want to prevent SQL injection", "Never"], answerIndex: 1, explanation: "It returns true if the first result is a ResultSet, and false if it is an update count." },
        { id: "jdbc-3-11", question: "How do you retrieve the auto-generated primary key after an INSERT using PreparedStatement?", options: ["Call getLastId()", "Call getGeneratedKeys() after executing the statement, if the statement was created with RETURN_GENERATED_KEYS", "Query MAX(id) from the table", "It is returned by executeUpdate()"], answerIndex: 1, explanation: "Connection.prepareStatement(sql, Statement.RETURN_GENERATED_KEYS) allows you to fetch the generated ID." },
        { id: "jdbc-3-12", question: "What happens if you try to pass an SQL string to `PreparedStatement.executeQuery(String sql)`?", options: ["It works normally", "It compiles the query", "It throws an SQLException", "It ignores the parameters"], answerIndex: 2, explanation: "For PreparedStatement, the SQL is provided at creation. You must use the no-arg `executeQuery()` method." },
        { id: "jdbc-3-13", question: "How do you bind a null value to a PreparedStatement parameter?", options: ["pstmt.setString(1, null);", "pstmt.setNull(1, java.sql.Types.VARCHAR);", "Both A and B usually work", "pstmt.set(1, NULL)"], answerIndex: 2, explanation: "While `setNull` is the explicit and standard way, most drivers also allow passing `null` to `setString`, `setObject`, etc." },
        { id: "jdbc-3-14", question: "What is `Statement.setMaxRows(int max)` used for?", options: ["Limits the number of rows in the database table", "Limits the maximum number of rows a ResultSet can contain for this Statement", "Sets the batch size", "Sets the fetch size"], answerIndex: 1, explanation: "Any excess rows are silently dropped. It prevents enormous ResultSets from causing OutOfMemory errors." },
        { id: "jdbc-3-15", question: "What is `Statement.setQueryTimeout(int seconds)`?", options: ["The time before the connection drops", "The number of seconds the driver will wait for a Statement object to execute before throwing an SQLException", "The time to read the ResultSet", "The timeout for login"], answerIndex: 1, explanation: "It prevents queries from running indefinitely and blocking application threads." },
        { id: "jdbc-3-16", question: "Can a PreparedStatement prevent SQL injection if the parameters are used for table names or column names (e.g., SELECT ? FROM ?)?", options: ["Yes", "No, parameters can only be bound to literal data values, not database identifiers", "Only in Oracle", "Only for column names"], answerIndex: 1, explanation: "You cannot bind table or column names. If you must construct dynamic queries, you must validate and sanitize identifiers manually." },
        { id: "jdbc-3-17", question: "Which method is used to bind an arbitrary Java object to a parameter?", options: ["setAny()", "setObject(int parameterIndex, Object x)", "setValue()", "bindObject()"], answerIndex: 1, explanation: "The JDBC driver will attempt to convert the Java object to the corresponding SQL type." },
        { id: "jdbc-3-18", question: "What does the `Statement.clearParameters()` method do?", options: ["Deletes the query", "Clears the current parameter values immediately, useful when reusing the PreparedStatement in a loop", "Clears the ResultSet", "Closes the connection"], answerIndex: 1, explanation: "While parameters are generally overwritten, clearing them guarantees no leftover data from the previous execution." },
        { id: "jdbc-3-19", question: "What is the security advantage of PreparedStatement over String concatenation?", options: ["It encrypts the query", "It automatically escapes special characters (like quotes) in the input, preventing them from altering the SQL logic", "It hides the query from network sniffers", "It locks the table"], answerIndex: 1, explanation: "Because the query structure is pre-compiled, user input is treated strictly as data, neutralizing injection." },
        { id: "jdbc-3-20", question: "If a query returns no rows, what does `executeQuery()` return?", options: ["null", "An empty ResultSet object", "Throws a SQLException", "Returns an integer 0"], answerIndex: 1, explanation: "It returns a valid ResultSet object, but its `next()` method will immediately return false." }
      ]
    },
    {
      level: 4,
      title: "Level 4: Executing Queries & ResultSet",
      description: "Extracting data, cursor navigation, and scrollable/updatable ResultSets.",
      passScore: 16,
      questions: [
        { id: "jdbc-4-1", question: "What is the initial position of a ResultSet cursor?", options: ["At the first row", "Before the first row", "After the last row", "At a random row"], answerIndex: 1, explanation: "You must call `rs.next()` once to move the cursor to the first valid row of data." },
        { id: "jdbc-4-2", question: "What does `ResultSet.next()` return?", options: ["The data of the next row", "An integer index", "A boolean: true if the new current row is valid, false if there are no more rows", "A ResultSet object"], answerIndex: 2, explanation: "It is commonly used in a `while (rs.next()) { ... }` loop." },
        { id: "jdbc-4-3", question: "How do you retrieve a string value from a column named 'username'?", options: ["rs.get(\"username\")", "rs.getString(\"username\")", "rs.getValue(\"username\")", "rs.fetchString(\"username\")"], answerIndex: 1, explanation: "Getter methods in ResultSet are strongly typed and accept either a column name or column index." },
        { id: "jdbc-4-4", question: "If you retrieve data using an index (e.g., `rs.getString(1)`), what is the index of the first column?", options: ["0", "1", "-1", "Depends on the database"], answerIndex: 1, explanation: "Just like PreparedStatement parameters, ResultSet column indexes are 1-based." },
        { id: "jdbc-4-5", question: "What happens if a database column is NULL and you call `rs.getInt(\"age\")`?", options: ["It throws an exception", "It returns null", "It returns 0", "It returns -1"], answerIndex: 2, explanation: "Primitive getters return 0 (or false for booleans). You must use `rs.wasNull()` immediately after to check if the actual value was NULL." },
        { id: "jdbc-4-6", question: "How do you correctly check if the last read column value was actually SQL NULL?", options: ["if (rs.getInt(\"age\") == null)", "if (rs.wasNull())", "if (rs.isNull(\"age\"))", "if (rs.getObject(\"age\") == null)"], answerIndex: 1, explanation: "`wasNull()` reports whether the last column read had a value of SQL NULL." },
        { id: "jdbc-4-7", question: "What is a `TYPE_FORWARD_ONLY` ResultSet?", options: ["A ResultSet that only updates data", "The default type: the cursor can only move forward through the data (using next())", "A ResultSet that streams to the client", "A ResultSet that allows backwards scrolling"], answerIndex: 1, explanation: "It provides the best performance and lowest memory footprint." },
        { id: "jdbc-4-8", question: "How do you create a Scrollable ResultSet (allowing `previous()`, `absolute()`, etc.)?", options: ["Call rs.setScrollable(true)", "Specify `ResultSet.TYPE_SCROLL_INSENSITIVE` or `TYPE_SCROLL_SENSITIVE` when creating the Statement", "It is scrollable by default", "Use a ScrollStatement"], answerIndex: 1, explanation: "e.g., `conn.createStatement(ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY)`." },
        { id: "jdbc-4-9", question: "What is the difference between `TYPE_SCROLL_INSENSITIVE` and `TYPE_SCROLL_SENSITIVE`?", options: ["Sensitive allows updates; Insensitive does not", "Sensitive reflects changes made by others in the database while the ResultSet is open; Insensitive does not", "Insensitive is faster", "There is no difference"], answerIndex: 1, explanation: "Sensitive ResultSets are 'live' views of the database, but many drivers do not support them well." },
        { id: "jdbc-4-10", question: "What is an Updatable ResultSet?", options: ["A ResultSet that automatically commits transactions", "A ResultSet (created with `CONCUR_UPDATABLE`) that allows you to update, insert, or delete rows directly via Java code without writing SQL UPDATE commands", "A ResultSet that streams updates to the database", "An error"], answerIndex: 1, explanation: "You can use methods like `rs.updateString(\"name\", \"Bob\")` followed by `rs.updateRow()`." },
        { id: "jdbc-4-11", question: "Which method is used to insert a new row using an Updatable ResultSet?", options: ["rs.insertRow()", "rs.moveToInsertRow(), set values, then rs.insertRow()", "rs.createRow()", "rs.newRow()"], answerIndex: 1, explanation: "You move to a special buffer row, update the columns, and then commit the insert." },
        { id: "jdbc-4-12", question: "What does `Statement.setFetchSize(int rows)` do?", options: ["Limits the total number of rows returned", "Provides a hint to the JDBC driver about the number of rows that should be fetched from the database over the network at a time", "Increases database timeout", "Sets the batch size"], answerIndex: 1, explanation: "If fetch size is 100, the driver brings back 100 rows per network trip instead of loading all 1,000,000 rows into RAM at once, preventing OutOfMemoryErrors." },
        { id: "jdbc-4-13", question: "If you call `rs.absolute(5)`, what happens to the cursor?", options: ["It moves 5 rows forward", "It moves 5 rows backward", "It moves to the 5th row in the ResultSet", "It throws an exception if the ResultSet is forward-only"], answerIndex: 2, explanation: "Absolute positioning requires a scrollable ResultSet." },
        { id: "jdbc-4-14", question: "What does `rs.relative(2)` do?", options: ["Moves to row 2", "Moves the cursor 2 rows forward relative to the current position", "Moves to the 2nd to last row", "Moves up 2 rows"], answerIndex: 1, explanation: "It moves the cursor relative to where it currently is." },
        { id: "jdbc-4-15", question: "Which method returns the number of the current row (1-based)?", options: ["rs.getIndex()", "rs.getRow()", "rs.getCurrent()", "rs.position()"], answerIndex: 1, explanation: "`getRow()` returns the row number. If there is no current row, it returns 0." },
        { id: "jdbc-4-16", question: "What exception is thrown if you try to call `rs.getString(1)` BEFORE calling `rs.next()` for the first time?", options: ["NullPointerException", "IndexOutOfBoundsException", "SQLException (e.g., 'Before start of result set')", "IllegalStateException"], answerIndex: 2, explanation: "The cursor is positioned 'before the first row', which does not point to valid data." },
        { id: "jdbc-4-17", question: "What is `ResultSet.CONCUR_READ_ONLY`?", options: ["The default concurrency mode, meaning the ResultSet cannot be used to update the database", "A mode that allows reads but prevents other transactions from writing", "A mode that drops connections", "A locking mechanism"], answerIndex: 0, explanation: "It is the opposite of `CONCUR_UPDATABLE`." },
        { id: "jdbc-4-18", question: "Can you retrieve the same column value twice in succession (e.g., `rs.getString(1)` then `rs.getString(1)`)?", options: ["Yes, for most standard drivers", "No, it always throws an exception", "Only for blobs", "Only in forward-only ResultSets"], answerIndex: 0, explanation: "Yes, though for certain stream-based types (like getBinaryStream), reading it twice may not be supported." },
        { id: "jdbc-4-19", question: "If you execute a query that returns columns of unknown data types, which generic method can you use to retrieve the value?", options: ["rs.getAny()", "rs.getValue()", "rs.getObject()", "rs.getGeneric()"], answerIndex: 2, explanation: "`getObject(int columnIndex)` returns the value as a generic Java `Object`." },
        { id: "jdbc-4-20", question: "When iterating through a massive ResultSet, why is calling `rs.close()` in a finally block critical?", options: ["To format the data", "To immediately release the database cursor and memory resources held by the driver on the database server", "To commit the transaction", "To stop the Java application"], answerIndex: 1, explanation: "Database cursors are limited resources; failing to close ResultSets leads to 'maximum open cursors exceeded' errors." }
      ]
    },
    {
      level: 5,
      title: "Level 5: Transactions & Concurrency",
      description: "ACID properties, commit/rollback, savepoints, and isolation levels.",
      passScore: 16,
      questions: [
        { id: "jdbc-5-1", question: "What is a Transaction in a database?", options: ["A network transfer", "A sequence of one or more SQL operations treated as a single, indivisible logical unit of work", "A backup process", "An error log"], answerIndex: 1, explanation: "Transactions adhere to the ACID properties (Atomicity, Consistency, Isolation, Durability)." },
        { id: "jdbc-5-2", question: "How do you disable the default auto-commit behavior in JDBC to group multiple statements into one transaction?", options: ["conn.startTransaction();", "conn.setAutoCommit(false);", "conn.beginTransaction();", "stmt.setAutoCommit(false);"], answerIndex: 1, explanation: "Setting auto-commit to false means you must manually call commit() or rollback()." },
        { id: "jdbc-5-3", question: "Which method successfully finalizes a transaction, saving all changes to the database?", options: ["conn.save()", "conn.push()", "conn.commit()", "stmt.commit()"], answerIndex: 2, explanation: "`commit()` applies all changes made since the previous commit/rollback." },
        { id: "jdbc-5-4", question: "Which method undoes all changes made in the current transaction?", options: ["conn.undo()", "conn.rollback()", "conn.cancel()", "stmt.revert()"], answerIndex: 1, explanation: "`rollback()` is typically called in a catch block if a SQLException occurs." },
        { id: "jdbc-5-5", question: "What is a 'Savepoint' in JDBC?", options: ["A file on disk", "A marker within a transaction that allows you to roll back a portion of the transaction rather than the entire thing", "A commit marker", "A backup table"], answerIndex: 1, explanation: "You can create a savepoint and call `conn.rollback(savepoint)`." },
        { id: "jdbc-5-6", question: "How do you create a Savepoint?", options: ["conn.createSavepoint(\"Name\")", "conn.setSavepoint(\"Name\")", "stmt.savepoint()", "new Savepoint()"], answerIndex: 1, explanation: "`setSavepoint()` returns a java.sql.Savepoint object." },
        { id: "jdbc-5-7", question: "What is 'Dirty Read' anomaly?", options: ["Reading corrupted data", "When a transaction reads data that has been modified by another transaction but not yet committed", "Reading data twice", "Reading deleted data"], answerIndex: 1, explanation: "If the other transaction rolls back, the first transaction has read invalid data." },
        { id: "jdbc-5-8", question: "What is a 'Non-Repeatable Read' anomaly?", options: ["A query fails to execute", "When a transaction reads the same row twice, and gets different data each time because another transaction updated it in between", "When an insert fails", "When a select returns nothing"], answerIndex: 1, explanation: "The data changes during the course of your transaction." },
        { id: "jdbc-5-9", question: "What is a 'Phantom Read' anomaly?", options: ["Reading data that doesn't exist", "When a transaction re-executes a query returning a set of rows, and finds that another transaction has inserted/deleted rows matching the condition", "A ghost in the database", "Reading null values"], answerIndex: 1, explanation: "Unlike Non-Repeatable read (which is about changed values), Phantom Read is about rows appearing/disappearing." },
        { id: "jdbc-5-10", question: "Which Transaction Isolation level provides the lowest level of isolation and allows Dirty Reads?", options: ["TRANSACTION_NONE", "TRANSACTION_READ_UNCOMMITTED", "TRANSACTION_READ_COMMITTED", "TRANSACTION_SERIALIZABLE"], answerIndex: 1, explanation: "Read Uncommitted does not place locks on reads." },
        { id: "jdbc-5-11", question: "Which Transaction Isolation level prevents Dirty Reads but allows Non-Repeatable and Phantom Reads?", options: ["TRANSACTION_READ_UNCOMMITTED", "TRANSACTION_READ_COMMITTED", "TRANSACTION_REPEATABLE_READ", "TRANSACTION_SERIALIZABLE"], answerIndex: 1, explanation: "Read Committed is the default isolation level for many databases like Oracle and PostgreSQL." },
        { id: "jdbc-5-12", question: "Which Transaction Isolation level prevents Dirty and Non-Repeatable Reads, but allows Phantom Reads?", options: ["TRANSACTION_READ_COMMITTED", "TRANSACTION_REPEATABLE_READ", "TRANSACTION_SERIALIZABLE", "TRANSACTION_NONE"], answerIndex: 1, explanation: "Repeatable Read is the default for MySQL/InnoDB." },
        { id: "jdbc-5-13", question: "Which Transaction Isolation level provides the highest level of isolation, preventing all three anomalies?", options: ["TRANSACTION_REPEATABLE_READ", "TRANSACTION_STRICT", "TRANSACTION_SERIALIZABLE", "TRANSACTION_MAX"], answerIndex: 2, explanation: "Serializable treats transactions as if they occurred sequentially, but severely impacts performance due to heavy locking." },
        { id: "jdbc-5-14", question: "How do you set the isolation level in JDBC?", options: ["conn.setIsolation()", "conn.setTransactionIsolation(Connection.TRANSACTION_SERIALIZABLE)", "stmt.setIsolationLevel()", "DriverManager.setIsolation()"], answerIndex: 1, explanation: "This tells the database how aggressively to lock records for this connection." },
        { id: "jdbc-5-15", question: "If you call `conn.close()` while a transaction is still open (auto-commit is false, and no commit/rollback was called), what happens?", options: ["It commits automatically", "It rolls back automatically", "It throws an exception", "The behavior is driver/database dependent, but it generally rolls back"], answerIndex: 3, explanation: "To ensure data integrity, always explicitly call commit() or rollback() before closing." },
        { id: "jdbc-5-16", question: "What does the ACID acronym stand for in database transactions?", options: ["Automatic, Consistent, Isolated, Distributed", "Atomicity, Consistency, Isolation, Durability", "Active, Concurrent, Independent, Durable", "Asynchronous, Committed, Interlocked, Defined"], answerIndex: 1, explanation: "These properties guarantee reliable processing of database transactions." },
        { id: "jdbc-5-17", question: "What does 'Atomicity' guarantee?", options: ["Speed of the transaction", "All operations within the transaction succeed together, or they all fail and roll back (All or Nothing)", "Data is isolated", "Data survives a crash"], answerIndex: 1, explanation: "If a bank transfer deducts money but fails to deposit it, Atomicity ensures the deduction is reversed." },
        { id: "jdbc-5-18", question: "What does 'Durability' guarantee?", options: ["The database won't crash", "Once a transaction is committed, it will remain committed even in the event of a system failure (e.g., power loss)", "Locks last forever", "Data is consistent"], answerIndex: 1, explanation: "The database writes the transaction to a non-volatile log before returning success." },
        { id: "jdbc-5-19", question: "Is it a good practice to perform long-running processes (like network calls or file processing) while holding a database transaction open?", options: ["Yes, to ensure consistency", "No, it holds database locks for a long time, leading to severe contention, blocking other users, and deadlocks", "It depends on the isolation level", "Yes, if using connection pooling"], answerIndex: 1, explanation: "Transactions should be as brief as possible." },
        { id: "jdbc-5-20", question: "If an exception is thrown in the middle of multiple executeUpdate calls (with auto-commit false), what must the catch block do?", options: ["Call conn.commit()", "Call conn.rollback()", "Call System.exit(1)", "Nothing, the database rolls back automatically"], answerIndex: 1, explanation: "The application is responsible for catching the error and explicitly issuing the rollback command to the connection." }
      ]
    },
    {
      level: 6,
      title: "Level 6: Batch Processing",
      description: "Executing multiple queries efficiently in a single network trip.",
      passScore: 16,
      questions: [
        { id: "jdbc-6-1", question: "What is the primary benefit of Batch Processing in JDBC?", options: ["It uses less memory", "It groups multiple SQL statements into a single network trip, significantly reducing network overhead and improving performance", "It prevents SQL injection", "It encrypts the queries"], answerIndex: 1, explanation: "Instead of making 1000 network round-trips for 1000 inserts, you make 1 trip." },
        { id: "jdbc-6-2", question: "Which method adds a SQL string to the batch of a standard Statement object?", options: ["addQuery()", "addBatch(String sql)", "appendBatch(String sql)", "pushQuery()"], answerIndex: 1, explanation: "e.g., `stmt.addBatch(\"INSERT INTO...\");`" },
        { id: "jdbc-6-3", question: "Which method adds a parameterized query to the batch of a PreparedStatement?", options: ["addBatch() (with no arguments)", "addBatch(String sql)", "addParameters()", "executeBatch()"], answerIndex: 0, explanation: "You set the parameters (e.g., `pstmt.setInt(1, 5);`), then call `pstmt.addBatch();` without providing the SQL again." },
        { id: "jdbc-6-4", question: "Which method sends the batch of commands to the database for execution?", options: ["runBatch()", "commitBatch()", "executeBatch()", "flushBatch()"], answerIndex: 2, explanation: "`executeBatch()` executes all commands added via `addBatch()`." },
        { id: "jdbc-6-5", question: "What does `executeBatch()` return?", options: ["A boolean", "A ResultSet", "An array of integers (int[])", "void"], answerIndex: 2, explanation: "Each integer in the array represents the update count (number of rows affected) for the corresponding command in the batch." },
        { id: "jdbc-6-6", question: "What does it mean if an element in the array returned by `executeBatch()` is `Statement.SUCCESS_NO_INFO` (-2)?", options: ["The command failed", "The command executed successfully, but the number of rows affected is unknown", "The command was skipped", "The database crashed"], answerIndex: 1, explanation: "Some databases cannot report the exact row count for batched updates." },
        { id: "jdbc-6-7", question: "What does it mean if an element in the array returned by `executeBatch()` is `Statement.EXECUTE_FAILED` (-3)?", options: ["The command failed to execute successfully", "The connection was lost", "The statement was a SELECT query", "No rows were updated"], answerIndex: 0, explanation: "This occurs if a command within the batch causes an error (though normally a BatchUpdateException is thrown)." },
        { id: "jdbc-6-8", question: "What exception is thrown if one of the commands in the batch fails?", options: ["SQLException", "BatchUpdateException", "QueryExecutionException", "DataIntegrityException"], answerIndex: 1, explanation: "BatchUpdateException extends SQLException and contains the update counts for the commands that succeeded before the failure." },
        { id: "jdbc-6-9", question: "Can a SELECT statement be included in a batch?", options: ["Yes, but it returns null", "No, batch processing is only for DML (Insert, Update, Delete) and DDL statements", "Yes, it returns a BatchResultSet", "Only in PreparedStatement"], answerIndex: 1, explanation: "Adding a SELECT query to a batch throws an exception because `executeBatch` expects update counts, not ResultSets." },
        { id: "jdbc-6-10", question: "Which method empties the current list of commands in the batch?", options: ["emptyBatch()", "removeBatch()", "clearBatch()", "resetBatch()"], answerIndex: 2, explanation: "If you decide not to execute the accumulated batch, `clearBatch()` removes them." },
        { id: "jdbc-6-11", question: "To maximize batch performance and maintain data integrity, what should you do with `auto-commit`?", options: ["Leave it true", "Set `auto-commit` to false, execute the batch, and then call `commit()`", "Use savepoints for every row", "Turn off the database"], answerIndex: 1, explanation: "This ensures the entire batch is treated as a single transaction (all or nothing) and improves speed by avoiding multiple commits." },
        { id: "jdbc-6-12", question: "Is it recommended to add 1,000,000 records to a batch before calling `executeBatch()`?", options: ["Yes, bigger is always better", "No, this will cause an OutOfMemoryError. You should execute and clear the batch in smaller chunks (e.g., every 1,000 or 5,000 rows).", "Yes, the driver handles memory automatically", "No, batch limit is 100"], answerIndex: 1, explanation: "Chunking the batch prevents exhausting the JVM heap and the database's incoming packet buffer." },
        { id: "jdbc-6-13", question: "When using PreparedStatement for batching, do you need to call `clearParameters()`?", options: ["Yes, always", "It is usually done automatically by `addBatch()`, but it is safe practice", "No, it deletes the SQL", "Only for strings"], answerIndex: 1, explanation: "`addBatch()` captures the current parameters, but clearing them avoids accidental reuse." },
        { id: "jdbc-6-14", question: "What is `rewriteBatchedStatements=true` in a MySQL JDBC URL?", options: ["It translates SQL to English", "A driver-specific optimization that rewrites multiple INSERT statements into a single, massive multi-value INSERT statement for extreme performance", "It disables batching", "It fixes syntax errors"], answerIndex: 1, explanation: "Without this parameter, MySQL JDBC driver often sends batch queries one by one under the hood, defeating the purpose of batching." },
        { id: "jdbc-6-15", question: "Can you mix different types of SQL statements (INSERT, UPDATE) in a `Statement` batch?", options: ["No", "Yes, a standard Statement can batch entirely different SQL strings", "Only if they target the same table", "Only in PreparedStatement"], answerIndex: 1, explanation: "Standard `Statement.addBatch(String)` accepts any DML command." },
        { id: "jdbc-6-16", question: "Can you mix different SQL statements in a `PreparedStatement` batch?", options: ["Yes", "No, a PreparedStatement is tied to a single SQL string at creation; you can only batch different parameter sets for that one string", "Only if you use dynamic SQL", "Only for deletes"], answerIndex: 1, explanation: "This is why PreparedStatement is faster; the DB only parses the query once." },
        { id: "jdbc-6-17", question: "If a BatchUpdateException is thrown, what does its `getUpdateCounts()` method return?", options: ["null", "An array indicating which commands succeeded and which failed (or were not executed)", "The total number of errors", "A string of errors"], answerIndex: 1, explanation: "This allows the application to know exactly how far the batch progressed before failing." },
        { id: "jdbc-6-18", question: "Does calling `executeBatch()` close the Statement?", options: ["Yes", "No, the Statement remains open and can be used for new batches or queries", "Only if it succeeds", "Only if auto-commit is true"], answerIndex: 1, explanation: "You must still call `stmt.close()` in a finally block." },
        { id: "jdbc-6-19", question: "If a batch contains 5 statements, and auto-commit is TRUE, what happens if the 3rd statement fails?", options: ["All 5 are rolled back", "Statements 1 and 2 are committed, 3 fails, and 4 and 5 are not executed (usually)", "The database crashes", "Statement 3 is skipped and 4 and 5 run"], answerIndex: 1, explanation: "Because auto-commit is true, the first two are permanent. This leaves the database in an inconsistent state, which is why auto-commit should be false for batches." },
        { id: "jdbc-6-20", question: "Which JDBC version officially introduced standard support for Batch Updates?", options: ["JDBC 1.0", "JDBC 2.0", "JDBC 3.0", "JDBC 4.0"], answerIndex: 1, explanation: "Batch updates were a major feature addition in the JDBC 2.0 API." }
      ]
    },
    {
      level: 7,
      title: "Level 7: CallableStatements & Stored Procedures",
      description: "Executing and retrieving data from database stored procedures and functions.",
      passScore: 16,
      questions: [
        { id: "jdbc-7-1", question: "What is a Stored Procedure?", options: ["A Java method stored on the disk", "A precompiled collection of SQL statements stored and executed on the database server", "A backup of the database", "A driver configuration file"], answerIndex: 1, explanation: "Stored procedures encapsulate business logic inside the database." },
        { id: "jdbc-7-2", question: "Which JDBC interface is used to execute Stored Procedures?", options: ["PreparedStatement", "ProcedureStatement", "CallableStatement", "Statement"], answerIndex: 2, explanation: "CallableStatement extends PreparedStatement to handle input and output parameters." },
        { id: "jdbc-7-3", question: "What is the standard SQL escape syntax used to call a stored procedure in JDBC?", options: ["call proc_name()", "{call proc_name(?, ?)}", "EXECUTE proc_name(?, ?)", "RUN proc_name(?, ?)"], answerIndex: 1, explanation: "The curly braces `{}` denote JDBC escape syntax, which the driver translates into the specific database's native call syntax." },
        { id: "jdbc-7-4", question: "Which method creates a CallableStatement?", options: ["conn.createStatement()", "conn.prepareCall(\"{call my_proc(?)}\")", "conn.createCall()", "conn.prepareStatement()"], answerIndex: 1, explanation: "`prepareCall()` prepares the escape string for execution." },
        { id: "jdbc-7-5", question: "What is an IN parameter?", options: ["A parameter whose value is returned by the procedure", "A parameter whose value is passed TO the procedure from the Java application", "An internal database variable", "An index"], answerIndex: 1, explanation: "IN parameters are set using `setInt()`, `setString()`, etc." },
        { id: "jdbc-7-6", question: "What is an OUT parameter?", options: ["A parameter ignored by the procedure", "A parameter used to return a value FROM the procedure back to the Java application", "A print statement", "An external database link"], answerIndex: 1, explanation: "OUT parameters must be registered before execution." },
        { id: "jdbc-7-7", question: "What is an INOUT parameter?", options: ["A parameter that acts as both an input value to the procedure and a variable to hold the output value", "A connection string", "A streaming parameter", "A syntax error"], answerIndex: 0, explanation: "It must be both set (with a value) and registered (for output)." },
        { id: "jdbc-7-8", question: "Before executing a CallableStatement that has an OUT parameter, what MUST you do?", options: ["Call setOut()", "Call registerOutParameter() specifying the parameter index and its SQL Type", "Call getResultSet()", "Call commit()"], answerIndex: 1, explanation: "e.g., `cstmt.registerOutParameter(2, java.sql.Types.INTEGER);`" },
        { id: "jdbc-7-9", question: "How do you retrieve the value of an OUT parameter after execution?", options: ["From the ResultSet", "Using getter methods on the CallableStatement (e.g., cstmt.getInt(2))", "It prints to console automatically", "Using request.getParameter()"], answerIndex: 1, explanation: "You call the appropriate getter on the CallableStatement using the parameter index or name." },
        { id: "jdbc-7-10", question: "Can a stored procedure return a ResultSet?", options: ["No, only OUT parameters", "Yes, it can return one or multiple ResultSets", "Only in Oracle", "Only if it has no OUT parameters"], answerIndex: 1, explanation: "Many databases allow procedures to execute `SELECT` queries that return cursors/ResultSets to the client." },
        { id: "jdbc-7-11", question: "If a CallableStatement returns both a ResultSet and OUT parameters, what must you do first?", options: ["Read the OUT parameters first", "Process and close all ResultSets before retrieving any OUT parameters", "It doesn't matter", "They are merged together"], answerIndex: 1, explanation: "For maximum portability across different databases, process all ResultSets before calling getters for OUT parameters." },
        { id: "jdbc-7-12", question: "How do you call a Database Function (which returns a single value) using JDBC escape syntax?", options: ["{call func_name(?)}", "{? = call func_name(?)}", "SELECT func_name(?)", "EXEC func_name()"], answerIndex: 1, explanation: "The first `?` acts as the return value (an OUT parameter), and the subsequent `?`s are arguments." },
        { id: "jdbc-7-13", question: "What class defines the constants used in `registerOutParameter` (e.g., INTEGER, VARCHAR)?", options: ["java.sql.Types", "java.sql.Constants", "javax.sql.Type", "java.db.Type"], answerIndex: 0, explanation: "The `Types` class defines generic SQL types." },
        { id: "jdbc-7-14", question: "Can you pass a null value to an IN parameter in a CallableStatement?", options: ["No", "Yes, using setNull(index, Types.VARCHAR)", "Only for strings", "Yes, by passing the word 'null'"], answerIndex: 1, explanation: "Like PreparedStatement, `setNull` safely passes a SQL NULL to the procedure." },
        { id: "jdbc-7-15", question: "Which method is generally used to execute a CallableStatement?", options: ["executeQuery() if it returns a ResultSet, executeUpdate() if it modifies data, or execute() if it does both or you are unsure", "run()", "commit()", "submit()"], answerIndex: 0, explanation: "Because procedures can do anything (read, write, return out params), `execute()` is the safest and most flexible method." },
        { id: "jdbc-7-16", question: "What is Named Parameter support in CallableStatement (JDBC 3.0+)?", options: ["Naming the database", "Setting/registering parameters by their string name instead of their integer index (e.g., cstmt.setInt(\"empId\", 5))", "Naming the statement", "Using variables in SQL"], answerIndex: 1, explanation: "This makes the Java code much more readable and independent of parameter ordering." },
        { id: "jdbc-7-17", question: "What is a REF CURSOR (common in Oracle) in JDBC?", options: ["A mouse cursor", "A database cursor returned as an OUT parameter, retrieved using `getObject()` and cast to a `ResultSet`", "A syntax error", "A reference to a table"], answerIndex: 1, explanation: "Oracle procedures typically use OUT parameters of type `Types.REF_CURSOR` to return sets of rows." },
        { id: "jdbc-7-18", question: "Why might a company choose to use CallableStatements over PreparedStatements?", options: ["They are written in Java", "To centralize complex business logic, reduce network traffic (one call executes many steps), and restrict direct table access", "Because they don't require SQL", "To bypass transactions"], answerIndex: 1, explanation: "Stored procedures can execute complex if/else logic entirely on the database server." },
        { id: "jdbc-7-19", question: "What does `cstmt.getMoreResults()` do?", options: ["Queries the database again", "Moves to the next ResultSet returned by a multiple-result-returning stored procedure", "Increases the fetch size", "Closes the current ResultSet"], answerIndex: 1, explanation: "If a procedure executes 3 SELECT statements, you use `getMoreResults()` to iterate through the 3 returned ResultSets." },
        { id: "jdbc-7-20", question: "If an error occurs inside a stored procedure, how is it communicated to the JDBC application?", options: ["The app crashes", "The procedure returns a null ResultSet", "The database throws an error which is translated into a `SQLException` in Java", "The connection is dropped"], answerIndex: 2, explanation: "Custom errors raised via `RAISE_APPLICATION_ERROR` (Oracle) or `SIGNAL` (SQL Server) are thrown as SQLExceptions." }
      ]
    },
    {
      level: 8,
      title: "Level 8: Database Metadata & Advanced Data Types",
      description: "Discovering schema info, handling LOBs, and Date/Time mapping.",
      passScore: 16,
      questions: [
        { id: "jdbc-8-1", question: "What is DatabaseMetaData?", options: ["Data about the user", "An interface providing comprehensive information about the database as a whole (tables, views, driver version, supported SQL features)", "A table of errors", "A backup file"], answerIndex: 1, explanation: "You get it via `Connection.getMetaData()`." },
        { id: "jdbc-8-2", question: "Which method would you use to get a list of all tables in the database?", options: ["conn.getTables()", "DatabaseMetaData.getTables()", "stmt.execute(\"SHOW TABLES\")", "ResultSet.getTables()"], answerIndex: 1, explanation: "`DatabaseMetaData.getTables(catalog, schemaPattern, tableNamePattern, types)` returns a ResultSet describing the tables." },
        { id: "jdbc-8-3", question: "What is ResultSetMetaData?", options: ["Metadata about the driver", "An interface providing information about the types and properties of the columns in a specific ResultSet", "A configuration file", "A metadata query string"], answerIndex: 1, explanation: "You get it via `ResultSet.getMetaData()`." },
        { id: "jdbc-8-4", question: "How do you find out how many columns are in a ResultSet?", options: ["rs.length()", "ResultSetMetaData.getColumnCount()", "rs.getColumns()", "DatabaseMetaData.getSize()"], answerIndex: 1, explanation: "This is extremely useful when writing generic code to print any query result." },
        { id: "jdbc-8-5", question: "How do you get the name of the first column in a ResultSet?", options: ["rs.getColumnName(1)", "ResultSetMetaData.getColumnName(1)", "ResultSetMetaData.getName(0)", "rs.getName(1)"], answerIndex: 1, explanation: "Column indexing in metadata, just like in the ResultSet itself, starts at 1." },
        { id: "jdbc-8-6", question: "What does BLOB stand for?", options: ["Binary Large Object", "Big Logic Object", "Base Level Output Binary", "Block Of Large Bytes"], answerIndex: 0, explanation: "BLOBs are used to store binary data like images, audio, or PDFs in the database." },
        { id: "jdbc-8-7", question: "What does CLOB stand for?", options: ["Character Large Object", "Common Logic Object", "Compiled Large Object Binary", "Char Literal Output Block"], answerIndex: 0, explanation: "CLOBs are used to store massive amounts of character/text data, like XML documents or articles." },
        { id: "jdbc-8-8", question: "How do you upload a file into a BLOB column using a PreparedStatement?", options: ["pstmt.setFile()", "pstmt.setBinaryStream(index, inputStream)", "pstmt.setString()", "pstmt.setBlob(index, File)"], answerIndex: 1, explanation: "You pass a `FileInputStream` to `setBinaryStream` or `setBlob`, and the driver streams the file to the database." },
        { id: "jdbc-8-9", question: "How do you retrieve an image from a BLOB column?", options: ["rs.getString()", "rs.getBinaryStream() or rs.getBlob()", "rs.getFile()", "rs.getImage()"], answerIndex: 1, explanation: "You retrieve an `InputStream` and write those bytes to a file or UI component." },
        { id: "jdbc-8-10", question: "Which `java.sql` class represents a date without time (Year, Month, Day)?", options: ["java.util.Date", "java.sql.Time", "java.sql.Date", "java.sql.Timestamp"], answerIndex: 2, explanation: "`java.sql.Date` extends `java.util.Date` but normalizes the time components to zero (midnight)." },
        { id: "jdbc-8-11", question: "Which `java.sql` class represents both Date AND Time, down to nanoseconds?", options: ["java.sql.Date", "java.sql.Time", "java.sql.Timestamp", "java.util.Calendar"], answerIndex: 2, explanation: "`Timestamp` maps to the SQL TIMESTAMP or DATETIME datatypes." },
        { id: "jdbc-8-12", question: "How do you safely pass a Java 8 `LocalDate` to a PreparedStatement in JDBC 4.2+?", options: ["You must convert it to a string", "You must convert it to java.sql.Date", "Use pstmt.setObject(index, localDate)", "Use pstmt.setDate(index, localDate)"], answerIndex: 2, explanation: "Modern JDBC drivers map `java.time.LocalDate` directly using `setObject()`." },
        { id: "jdbc-8-13", question: "What is an SQL ARRAY?", options: ["A string of comma-separated values", "A database datatype representing a collection of elements of the same type", "A JSON object", "A Java array"], answerIndex: 1, explanation: "Supported by databases like PostgreSQL, it maps to the `java.sql.Array` interface." },
        { id: "jdbc-8-14", question: "How do you retrieve an SQL ARRAY from a ResultSet?", options: ["rs.getList()", "rs.getArray(\"columnName\")", "rs.getString()", "rs.getObjects()"], answerIndex: 1, explanation: "You can then call `getArray()` on the `java.sql.Array` object to get a standard Java array." },
        { id: "jdbc-8-15", question: "What is the `java.sql.Types` class?", options: ["A class of exceptions", "A class defining constants that are used to identify generic SQL types (e.g., Types.INTEGER, Types.VARCHAR)", "A formatting tool", "A database engine"], answerIndex: 1, explanation: "These constants are used in methods like `setNull()` and `registerOutParameter()`." },
        { id: "jdbc-8-16", question: "If you call `ResultSetMetaData.getColumnType(1)`, what does it return?", options: ["A string like 'VARCHAR'", "An int corresponding to a constant in java.sql.Types", "A Java Class object", "A boolean"], answerIndex: 1, explanation: "To get the database-specific name (like 'VARCHAR2'), use `getColumnTypeName(1)`." },
        { id: "jdbc-8-17", question: "How do you handle JSON data stored in a PostgreSQL JSONB column using JDBC?", options: ["Use rs.getJSON()", "Retrieve it as a String using rs.getString(), or use rs.getObject() with an appropriate library (like Gson/Jackson) to parse it", "JDBC cannot read JSON", "Use rs.getBlob()"], answerIndex: 1, explanation: "JDBC treats it as a string or a PGObject, which you then parse using standard Java JSON libraries." },
        { id: "jdbc-8-18", question: "Can you retrieve the primary keys of a table dynamically without knowing them beforehand?", options: ["No", "Yes, using DatabaseMetaData.getPrimaryKeys(catalog, schema, table)", "Only by parsing the SQL", "Only in MySQL"], answerIndex: 1, explanation: "This is how ORM frameworks (like Hibernate) map tables automatically." },
        { id: "jdbc-8-19", question: "What is a Clob `getCharacterStream()` useful for?", options: ["Downloading music", "Reading a massive text document from the database directly into a file or processor without loading the whole string into RAM", "Writing to the database", "Parsing JSON"], answerIndex: 1, explanation: "Streaming prevents OutOfMemory errors on large payloads." },
        { id: "jdbc-8-20", question: "What does the `DatabaseMetaData.supportsTransactions()` method do?", options: ["Starts a transaction", "Returns true if the database and driver support ACID transactions", "Commits data", "Disables auto-commit"], answerIndex: 1, explanation: "Not all databases (e.g., older MySQL MyISAM tables or flat-file databases) support transactions." }
      ]
    },
    {
      level: 9,
      title: "Level 9: Connection Pooling & DataSources",
      description: "JNDI, performance benefits, and third-party connection pools.",
      passScore: 16,
      questions: [
        { id: "jdbc-9-1", question: "Why is opening a new database connection for every request a severe anti-pattern in web applications?", options: ["It confuses the user", "Establishing a physical TCP/IP network connection and authenticating is a very slow and resource-heavy process", "It causes SQL injection", "It is not allowed by Java"], answerIndex: 1, explanation: "Frequent connection creation destroys application performance and overloads the database server." },
        { id: "jdbc-9-2", question: "What is Connection Pooling?", options: ["A swimming pool for cables", "A cache of active, ready-to-use database connections maintained in memory that applications can borrow and return", "A method to compress data", "A routing protocol"], answerIndex: 1, explanation: "When an app needs a connection, it borrows one from the pool. When done, it returns it to the pool instead of closing it." },
        { id: "jdbc-9-3", question: "Which interface in the `javax.sql` package is the standard API for interacting with Connection Pools?", options: ["DriverManager", "ConnectionPool", "DataSource", "PoolManager"], answerIndex: 2, explanation: "Applications call `DataSource.getConnection()` to get a pooled connection." },
        { id: "jdbc-9-4", question: "What happens when you call `close()` on a Connection object obtained from a Connection Pool?", options: ["The physical TCP connection to the database is permanently severed", "The connection is returned to the pool for the next thread to use", "An exception is thrown", "The database shuts down"], answerIndex: 1, explanation: "The connection pool wraps the real Connection in a proxy. Calling close() just signals the pool that you are done." },
        { id: "jdbc-9-5", question: "What is JNDI (Java Naming and Directory Interface)?", options: ["A database engine", "An API that allows Java applications to discover and look up data and objects (like a pre-configured DataSource) via a name", "A web server", "A security framework"], answerIndex: 1, explanation: "Application servers (like Tomcat) create the connection pool and bind it to a JNDI name (e.g., `java:comp/env/jdbc/myDB`)." },
        { id: "jdbc-9-6", question: "Why is using JNDI with a DataSource advantageous in enterprise applications?", options: ["It writes the SQL queries", "It decouples the application code from database credentials and driver details, shifting configuration to the Application Server", "It bypasses firewalls", "It compiles faster"], answerIndex: 1, explanation: "The Ops team can change the DB password in Tomcat without touching or recompiling the Java code." },
        { id: "jdbc-9-7", question: "Which of the following is a highly popular, extremely fast, production-ready third-party Connection Pool library?", options: ["HikariCP", "JDBC-Pool", "JavaPool", "DriverManager"], answerIndex: 0, explanation: "HikariCP is the default connection pool in Spring Boot due to its zero-overhead reliability and raw speed." },
        { id: "jdbc-9-8", question: "What happens if a connection pool is exhausted (all connections are currently in use by other threads)?", options: ["The database crashes", "The application creates a new connection instantly", "The calling thread blocks (waits) until a connection is returned, or a timeout occurs", "The thread is killed"], answerIndex: 2, explanation: "This prevents the database from being overwhelmed, acting as a throttling mechanism (Connection Timeout)." },
        { id: "jdbc-9-9", question: "What is a 'Connection Leak'?", options: ["Data leaking over the network", "When an application borrows a connection from the pool but forgets to call close() (return it), eventually exhausting the pool", "When the pool creates too many connections", "A security breach"], answerIndex: 1, explanation: "Connection leaks are a major cause of application downtime. Always use try-with-resources." },
        { id: "jdbc-9-10", question: "What does the `maximumPoolSize` (or maxActive) setting do?", options: ["Limits the size of the database", "Defines the absolute maximum number of physical connections the pool will ever create to the database", "Sets the timeout duration", "Limits the query length"], answerIndex: 1, explanation: "This protects the database server from running out of memory or connection slots." },
        { id: "jdbc-9-11", question: "What does the `minimumIdle` (or initialSize) setting do?", options: ["The time before a connection drops", "The number of connections the pool will keep open and ready even when there is no traffic", "The minimum queries allowed", "The timeout limit"], answerIndex: 1, explanation: "Keeping idle connections prevents latency spikes when traffic suddenly arrives." },
        { id: "jdbc-9-12", question: "Why do connection pools perform 'Validation Queries' (or use Connection.isValid)?", options: ["To check if the SQL syntax is correct", "To ping the database and ensure a connection hasn't been dropped by a firewall or network glitch before giving it to the application", "To validate the user login", "To measure network speed"], answerIndex: 1, explanation: "This prevents the application from receiving 'Broken Pipe' exceptions." },
        { id: "jdbc-9-13", question: "Is `DriverManager` capable of Connection Pooling natively?", options: ["Yes, if configured", "No, DriverManager only creates basic, unpooled physical connections", "Only in Java 11+", "Yes, using threads"], answerIndex: 1, explanation: "This is why `DataSource` implementations (like HikariCP) are used instead." },
        { id: "jdbc-9-14", question: "What is the `javax.sql.ConnectionEvent` used for?", options: ["To log errors", "To notify a registered ConnectionEventListener that a pooled connection has been closed or generated a fatal error", "To execute queries", "To connect to the internet"], answerIndex: 1, explanation: "The underlying connection pool uses this to know when to recycle or destroy connections." },
        { id: "jdbc-9-15", question: "Can a DataSource be used in a standalone Java application (without Tomcat/JBoss)?", options: ["No, it requires an Application Server", "Yes, you can instantiate a DataSource (like HikariDataSource) directly in your code", "Only via JNDI", "Only if the database is local"], answerIndex: 1, explanation: "While JNDI is common in Java EE, standalone apps (and Spring Boot) configure the DataSource programmatically." },
        { id: "jdbc-9-16", question: "What is `idleTimeout` in a connection pool?", options: ["The time a user can stay logged in", "The maximum amount of time a connection is allowed to sit idle in the pool before being retired/closed to free up resources", "The query timeout", "The login timeout"], answerIndex: 1, explanation: "This prevents holding open connections to the database unnecessarily during low-traffic periods." },
        { id: "jdbc-9-17", question: "What is `maxLifetime` in a connection pool (like HikariCP)?", options: ["The lifespan of the application", "The maximum lifetime of a connection in the pool before it is forcefully retired and replaced, mitigating slow database memory leaks", "The length of a transaction", "The time to execute a query"], answerIndex: 1, explanation: "Retiring old connections keeps the network and database healthy." },
        { id: "jdbc-9-18", question: "What is an `XADataSource`?", options: ["An XML database source", "A factory for XA connections, used in distributed (Two-Phase Commit) transactions across multiple different databases", "An expired datasource", "An X-axis data chart"], answerIndex: 1, explanation: "If you need to update an Oracle DB and a PostgreSQL DB in the exact same transaction, you use XA." },
        { id: "jdbc-9-19", question: "How does a framework like Spring interact with JDBC DataSources?", options: ["It rewrites them", "It injects the DataSource into a `JdbcTemplate` which handles the boilerplate of creating, executing, and safely closing connections", "It deletes them", "It doesn't use JDBC"], answerIndex: 1, explanation: "Spring abstracts away the pain of `try/catch/finally` blocks for connections." },
        { id: "jdbc-9-20", question: "If your application gets a `ConnectionTimeoutException` (e.g., 'Timeout waiting for connection from pool'), what is the most likely culprit?", options: ["The database is offline", "A connection leak in the code (connections aren't being closed) or the pool size is too small for the traffic", "The SQL syntax is wrong", "The password expired"], answerIndex: 1, explanation: "If threads borrow connections but don't return them, the pool empties, and new requests wait until they time out." }
      ]
    },
    {
      level: 10,
      title: "Level 10: Best Practices, RowSets & Exceptions",
      description: "try-with-resources, disconnected RowSets, SQLWarnings, and clean code.",
      passScore: 16,
      questions: [
        { id: "jdbc-10-1", question: "What is the most critical rule when working with JDBC resources (Connection, Statement, ResultSet)?", options: ["Use public variables", "Always explicitly close them in a `finally` block or use `try-with-resources` to prevent resource leaks", "Never use transactions", "Only use Type 1 drivers"], answerIndex: 1, explanation: "Relying on the Garbage Collector to clean up database connections is a recipe for system crashes." },
        { id: "jdbc-10-2", question: "How does the Java 7 `try-with-resources` statement simplify JDBC code?", options: ["It writes the SQL", "It automatically calls `close()` on the declared resources at the end of the block, even if an exception is thrown", "It catches all errors silently", "It automatically commits transactions"], answerIndex: 1, explanation: "It replaces ugly, nested `try/catch/finally` blocks. Resources must implement `AutoCloseable`." },
        { id: "jdbc-10-3", question: "What is a `RowSet` in JDBC?", options: ["A row in a table", "An interface that extends `ResultSet` to provide a JavaBean, making it easier to pass data over the network or use disconnected", "A set of columns", "A database index"], answerIndex: 1, explanation: "RowSets wrap ResultSets into more flexible, manageable components." },
        { id: "jdbc-10-4", question: "What is the primary feature of a `CachedRowSet`?", options: ["It caches queries", "It is disconnected; it retrieves data, closes the connection, and holds the data in memory for the application to use offline", "It requires a constant connection", "It caches the database schema"], answerIndex: 1, explanation: "This is highly useful for passing data to a UI layer without holding a database connection open." },
        { id: "jdbc-10-5", question: "How does a `CachedRowSet` update the database?", options: ["It writes to disk", "The application modifies the disconnected data, then calls `acceptChanges()`, which reconnects and syncs the updates", "It automatically updates via magic", "It cannot update the database"], answerIndex: 1, explanation: "It acts as a detached buffer, syncing back via Optimistic Concurrency." },
        { id: "jdbc-10-6", question: "What is a `WebRowSet`?", options: ["A browser", "An extension of CachedRowSet that can read and write its data in XML format, useful for web services", "An HTML table", "A networking protocol"], answerIndex: 1, explanation: "It serializes the data and metadata to XML easily." },
        { id: "jdbc-10-7", question: "What is a `SQLWarning`?", options: ["A fatal error", "A subclass of SQLException that provides information about a non-fatal warning (e.g., data truncation)", "A compile-time error", "A warning from the Java compiler"], answerIndex: 1, explanation: "Warnings don't interrupt execution. You retrieve them using `stmt.getWarnings()` or `conn.getWarnings()`." },
        { id: "jdbc-10-8", question: "When iterating through a ResultSet, should you access columns by name or by index for maximum performance?", options: ["By name", "By index (e.g., rs.getString(1))", "Performance is exactly identical", "By using a Hash Map"], answerIndex: 1, explanation: "Accessing by index avoids the driver having to do a string lookup for the column name on every row, though by name is more readable." },
        { id: "jdbc-10-9", question: "What does `SQLException.getErrorCode()` return?", options: ["The line of code that failed", "A vendor-specific integer error code (e.g., 1045 for MySQL access denied)", "The HTTP status", "The JVM error code"], answerIndex: 1, explanation: "Because it is vendor-specific, it shouldn't be heavily relied upon for cross-database compatibility." },
        { id: "jdbc-10-10", question: "What does `SQLException.getSQLState()` return?", options: ["The database name", "A standard X/Open or SQL:2003 code indicating the type of error (e.g., '23000' for constraint violation)", "The state of the connection", "The region of the server"], answerIndex: 1, explanation: "SQLState codes are somewhat standardized across different database vendors." },
        { id: "jdbc-10-11", question: "If you need to execute a dynamic query where the table name itself changes based on user input, can you use `PreparedStatement` parameters?", options: ["Yes, use ? for the table name", "No, parameters only bind to values. You must use String concatenation and rigorously validate/whitelist the input.", "Yes, use setTableName()", "Only in Oracle"], answerIndex: 1, explanation: "This is a common pitfall. Table and column identifiers cannot be parameterized." },
        { id: "jdbc-10-12", question: "What is an `SQLFeatureNotSupportedException`?", options: ["A syntax error", "An exception thrown when the JDBC driver does not support an optional JDBC feature (e.g., certain scrollable ResultSets)", "The database is turned off", "The Java version is too old"], answerIndex: 1, explanation: "Drivers are not required to implement the entire JDBC specification." },
        { id: "jdbc-10-13", question: "In a layered architecture (MVC/DAO), where should JDBC code strictly reside?", options: ["In the View (JSP/UI)", "In the Controller (Servlet)", "In the Data Access Object (DAO) or Repository layer", "In the Entity classes"], answerIndex: 2, explanation: "The DAO pattern encapsulates and hides all database interaction details from the rest of the application." },
        { id: "jdbc-10-14", question: "What is 'Object-Relational Impedance Mismatch'?", options: ["A slow network", "The conceptual and technical difficulties that occur when mapping object-oriented models (Java) to relational structures (SQL tables)", "An electrical fault", "A driver error"], answerIndex: 1, explanation: "Frameworks like Hibernate/JPA exist specifically to solve this mismatch by abstracting JDBC." },
        { id: "jdbc-10-15", question: "Why is it generally bad practice to write `SELECT *` in JDBC applications?", options: ["It causes compile errors", "It fetches unnecessary data wasting network/memory, and breaks if column orders change and you use index-based getters", "It locks the table", "It prevents caching"], answerIndex: 1, explanation: "Always explicitly define the columns you need (`SELECT id, name FROM...`)." },
        { id: "jdbc-10-16", question: "What is `DataTruncation`?", options: ["Deleting a table", "A subclass of SQLWarning/SQLException thrown when data is unexpectedly truncated on a read or write (e.g., string is too long for the column)", "Formatting a disk", "Rounding a number"], answerIndex: 1, explanation: "If it happens on a read, it's usually a warning; on a write, it's an exception." },
        { id: "jdbc-10-17", question: "What does `Connection.setReadOnly(true)` do?", options: ["Encrypts the connection", "Provides a hint to the database that the connection will only be used for reads, allowing the DB to optimize the execution", "Prevents SQL injection", "Makes the Java variables final"], answerIndex: 1, explanation: "It does not guarantee writes will fail, but it enables deep optimizations for caching and routing." },
        { id: "jdbc-10-18", question: "How should you handle transactions when multiple DAOs are involved in a single logical business operation?", options: ["Each DAO commits its own transaction", "Pass the same Connection object to all DAOs from a Service layer, and commit the Connection at the Service level", "Use multiple connections", "Don't use transactions"], answerIndex: 1, explanation: "Transaction boundaries belong in the Business/Service layer to ensure Atomicity across multiple database operations." },
        { id: "jdbc-10-19", question: "What does `PreparedStatement.addBatch()` do if called inside a loop over a large dataset?", options: ["Executes the query instantly", "Queues the parameters for later execution via `executeBatch()`, keeping network trips low", "Creates multiple Statements", "Closes the connection"], answerIndex: 1, explanation: "Batching is a mandatory best practice for bulk inserts/updates." },
        { id: "jdbc-10-20", question: "What is a `JoinRowSet`?", options: ["An SQL JOIN command", "A RowSet interface implementation that allows you to join data from different RowSets in Java memory without using SQL", "A string concatenator", "A network link"], answerIndex: 1, explanation: "It essentially performs a relational JOIN on the client side." }
      ]
    }
  ]
};