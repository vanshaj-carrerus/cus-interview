import TopicTutorPage from "../components/topic-tutor-page";
import { systemDesignData } from "../data/system-design-data";

export default function SystemDesignPage() {
  return <TopicTutorPage topic={systemDesignData} />;
}
