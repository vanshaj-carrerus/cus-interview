import TopicTutorPage from "../components/topic-tutor-page";
import { scriptingData } from "../data/scripting-data";

export default function ScriptingPage() {
  return <TopicTutorPage topic={scriptingData} />;
}
